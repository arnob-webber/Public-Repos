function arrReverse(arr) {
  return arr.reverse();
}

console.log(arrReverse([1, 2, 3, 4, 5]));

// If the array has only one element, reverse() technically still works, but the result
//  remains the same because there’s nothing to swap.
