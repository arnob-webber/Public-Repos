// Write a function flattenArray that flattens a nested array into a single-level array using the flat() method.

function flatArr(arr) {
  return arr.flat();
}

console.log(flatArr([1, [2], [3, 5]]));

// u use this to avoid Unnecessary Nesting:
