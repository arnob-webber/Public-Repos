// Write a program to sum all elements in an array.

let myArray = [1, 2, 3, 4, 5, 6];
let y = 0;
myArray.forEach((x) => (y += x));
console.log(y);

// let myArray = [1, 2, 3, 4, 5, 6];
// let y = 0;
// myArray.forEach((x) => (y += x));
// console.log(y);
